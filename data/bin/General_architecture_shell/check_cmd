check_cmd() {
    local cmd=$1
    total=$((total+1))
    if command -v "$cmd" >/dev/null 2>&1; then
        echo -e "${GREEN}[✓]${NC} $cmd"
        success=$((success+1))
    else
        echo -e "${RED}[✗]${NC} $cmd (未找到)"
        fail=$((fail+1))
    fi
}